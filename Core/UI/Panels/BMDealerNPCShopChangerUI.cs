namespace QoLCompendium.Core.UI.Panels
{
    /*
    public class BMDealerNPCShopChangerUI : UIState
    {
        public static bool visible = false;
        public static uint timeStart;

        public override void OnInitialize()
        {
            UIPanel MainPanel = new();
            MainPanel.SetPadding(0);
            MainPanel.HAlign = 0.628f;
            MainPanel.VAlign = 0.53f;
            MainPanel.Width.Set(360f, 0f);
            MainPanel.Height.Set(40f, 0f);
            //GlobePanel.BackgroundColor *= 0f;
            //GlobePanel.BorderColor *= 0f;
            Append(MainPanel);
        }
    }
    */
}
